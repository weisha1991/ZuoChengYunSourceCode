#ifndef MANACHER_H
#define MANACHER_H
#include "publicHeader.h"
#include <algorithm>
using std::string;
using std::cout;
using std::endl;

string manacherString(string str);
int maxLongestPalidromeStringLen(string str);

#endif
